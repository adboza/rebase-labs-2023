version: '3.8'

services:
  app: 
    container_name: rebase-labs
    restart: always
    image: ruby
    working_dir: /app
    volumes: 
      - .:/app
      - rubygems_rebase_labs:/usr/local/bundle
      - gemconfig:/root/.local/share/gem
    links:
      - postgres
    networks:
      - rebase-labs
    ports:
      - '3000:3000'
    command: bash -c "bundle && ruby server.rb"
  postgres:
    container_name: postgres
    image: postgres
    volumes:
      - pgdata_rebase_labs:/var/lib/postgresql/data
    environment:
      POSTGRES_HOST: 'postgres'
      POSTGRES_HOST_AUTH_METHOD: 'trust'
      POSTGRES_DB: 'medical_records'
    networks:
      - rebase-labs

  # selenium:                                        # Selenium container
  #   image: selenium/standalone-chrome
  #   ports:
  #     - "4444:4444"
  #   logging:                                       # Disable spamming irrelevant messages from this container
  #     driver: none
  #   networks:
  #     - rebase-labs                               # Container name in virtual network that can be addressed
  # chromedriver:
  #   container_name: chromedriver
  #   image: openstax/selenium-chrome
  #   working_dir: /app
  #   volumes: 
  #     - .:/app
  #     - rubygems_rebase_labs:/usr/local/bundle
  #     - gemconfig:/root/.local/share/gem
  #   links:
  #     - app
  #   networks:
  #     - rebase-labs
  #   ports:
  #     - '4444:4444'

volumes:
  rubygems_rebase_labs:
  gemconfig:
  pgdata_rebase_labs:
networks:
  rebase-labs: {}